echo "=== สินค้าทั้งหมดใน Docker Database ===" && docker exec skp-postgres psql -U postgres -d skp_stock -c "SELECT sku, barcode, name FROM products ORDER BY sku;" && echo "" && echo "บันทึกข้อมูลลงไฟล์..." && docker exec skp-postgres psql -U postgres -d skp_stock -t -A -F"," -c "SELECT sku, barcode, name FROM products ORDER BY sku;" > current-barcodes.csv && echo "✅ บันทึกไว้ที่ current-barcodes.csv"